#include "cub3d.h"

char **parse_map(void);
{
    // allocate for **
    // get next line, allocate for each * and store the read line
    // maybe store the longest line length to determine the cube size. in a passed by adress arg. 
}